# Solana mainnet RPC URL used to sign + submit test transactions.
# Required. Use a dedicated endpoint (Helius / QuickNode / etc) — the
# public RPC will rate-limit the test suite.
RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
